function Input({ type, id }) {
  return (
    <input
      type={type}
      id={id}
      className="w-full px-3 py-2 rounded-mg bg-white text-black border border-current"
    />
  );
}

export default Input;
