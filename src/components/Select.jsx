function Select() {
  return (
    <select className="w-full px-3 py-2 rounded-mg bg-white text-black border border-current">
      <option value="someOption">Some option</option>
      <option value="otherOption">Other option</option>
    </select>
  );
}

export default Select;
